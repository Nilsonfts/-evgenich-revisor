# help_system.py
"""
Система справки «ЕВГЕНИЧ-РЕВИЗОР» — с характером и юмором 🗿
"""

from datetime import datetime
from roles import get_available_roles_for_day, UserRole

class HelpSystem:
    """Система справки для бота с поддержкой ролей и скуф-харизмой."""
    
    @staticmethod
    def get_user_help() -> str:
        """Возвращает справку для обычных пользователей."""
        day_of_week = datetime.now().weekday()
        is_weekend = day_of_week in [4, 5]  # Пт-Сб
        day_name = ["понедельник", "вторник", "среда", "четверг", "пятница", "суббота", "воскресенье"][day_of_week]
        
        help_text = f"""
🗿 **ЕВГЕНИЧ-РЕВИЗОР** — справка для ведущих

📅 *Сегодня: {day_name.upper()}*
"""
        
        if is_weekend:
            help_text += """
🎉 **ВЫХОДНЫЕ (ПТ-СБ) — двойная 'гойда'!**

🎤 **Караоке ведущий** — капитан вечера
   Цель: 15 голосовых • Ведёт программу

🎭 **МС (аниматор)** — душа компании
   Цель: 10 голосовых • Игры и конкурсы

💡 `/start` — займёт свободную роль
   `/start караоке` / `/start МС` — выбрать конкретную
"""
        else:
            help_text += """
🎤 **БУДНИ (ВС-ЧТ) — один ведущий, одна легенда**

🎤 **Караоке ведущий** — единственная роль
   Цель: 15 голосовых • Полная программа

💡 `/start` — заступить на смену
"""
        
        help_text += """
━━━━━━━━━━━━━━━━━━━━━

🎯 **ОСНОВНЫЕ КОМАНДЫ**

📊 `/check` — промежуточный отчёт по смене
🏁 `/gameover` — завершить смену (после окончания рабочего времени)
📈 `/сводка` — общая статистика за всё время
🔄 `/передать` — передать смену (ответом на сообщение)

⏸️ **ПАУЗЫ И ПЕРЕРЫВЫ**
• `/пауза` — остановить все счётчики на 40 мин
• `/стоп_пауза` — снять паузу досрочно
• Голосовое во время паузы автоматически её снимает

☕️ **ПЕРЕРЫВ** — просто напишите ключевое слово:
   _перерыв, обед, покурить, кофе, устал, отойду, туалет..._

✅ **ВОЗВРАЩЕНИЕ** — напишите или отправьте голосовое:
   _вернулся, готов, работаю, на месте, поехали..._

🎤 **ГОЛОСОВЫЕ** — отправляйте и они считаются автоматически

━━━━━━━━━━━━━━━━━━━━━

📋 **ЕЩЁ КОМАНДЫ**
• `/help` — эта справка
• `/commands` — полный список команд
• `/роли` — система ролей и расписание
• `/настройки` — настройки чата
• `/time` — текущее время
• `/rating` — рейтинг ведущих
• `/status` — статус системы

🏁 **ЗАВЕРШЕНИЕ СМЕНЫ**
• `/gameover` — только после окончания рабочего времени
   _Сформирует финальный отчёт и закроет смену_

💪 **Удачной смены! Евгенич следит за тобой** 👁️
"""
        
        return help_text
    
    @staticmethod
    def get_admin_help() -> str:
        """Возвращает справку для администраторов."""
        return """
⚜️ **ЕВГЕНИЧ-РЕВИЗОР — админ-мануал** ⚜️

🎛️ **ГЛАВНОЕ ОРУЖИЕ:**
• `/admin` — интерактивная панель (кнопки > команды)
• `/setup_wizard` — мастер настройки чата

🔧 **УПРАВЛЕНИЕ БОТОМ:**
• `/bot_off` / `/bot_on` — вкл/выкл бота
• `/bot_status` — проверить статус
• `/restart` — сброс текущей смены
• `/problems` — диагностика ошибок

📊 **ОТЧЁТЫ И АНАЛИТИКА:**
• `/report` — детальный отчёт
• `/rating` — рейтинг ведущих
• `/status` — статус системы
• `/log` — журнал событий
• `/marketing_analytics` — маркетинговая аналитика

📢 **BOSS-ФУНКЦИИ (только BOSS\\_ID):**
• `/broadcast` — рассылка во все чаты

🔧 **ТЕХНИЧЕСКОЕ:**
• `/debug_config` — отладка конфигурации
• `/ads` — управление рекламными шаблонами
• `/adminhelp` — эта справка

━━━━━━━━━━━━━━━━━━━━━

🗓️ **РАСПИСАНИЕ РОЛЕЙ:**
• *Вс-Чт:* 1 караоке ведущий (цель: 15)
• *Пт-Сб:* караоке (15) + МС (10)

💾 **БАЗА ДАННЫХ:**
• SQLite/PostgreSQL • Авто-очистка 30+ дней
• Бэкап в Google Таблицы

🔄 **АВТОМАТИЗАЦИЯ:**
• Очистка БД в 03:00
• Авто-назначение ролей
• Интеграция Google Sheets

💡 *Используйте /admin — там кнопки, там удобно!*
"""
    
    @staticmethod
    def get_roles_info() -> str:
        """Возвращает информацию о системе ролей."""
        day_of_week = datetime.now().weekday()
        day_names = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]
        current_day = day_names[day_of_week]
        
        return f"""
🎭 **СИСТЕМА РОЛЕЙ** 🎭

📅 *Сегодня: {current_day}*

🗓️ **РАСПИСАНИЕ:**

🔹 **Будни (Вс-Чт):**
   🎤 Караоке ведущий × 1
   Цель: 15 голосовых

🔸 **Выходные (Пт-Сб):**
   🎤 Караоке ведущий — 15 голосовых
   🎭 МС (аниматор) — 10 голосовых
   _Работают параллельно!_

⚙️ **НАЗНАЧЕНИЕ В ВЫХОДНЫЕ:**
• Первое голосовое = караоке ведущий
• Второе голосовое = МС
• Или вручную: `/start караоке` / `/start МС`

📊 **РАЗДЕЛЬНАЯ ОТЧЁТНОСТЬ:**
• У каждой роли своя статистика
• Отдельные цели и показатели
• Независимые отчёты в Google Таблицы
"""

# Быстрые функции для обработчиков
def get_help_text() -> str:
    return HelpSystem.get_user_help()

def get_admin_help_text() -> str:
    return HelpSystem.get_admin_help()

def get_roles_help_text() -> str:
    return HelpSystem.get_roles_info()
